const initialState = {}

const ndChange = (state = initialState, action) => {
  switch (action.type) {
    // case 'ND_CHANGE':
      // delete action.type;
      // return Object.assign({}, state, action)
    default: return state
  }
}

export default ndChange
